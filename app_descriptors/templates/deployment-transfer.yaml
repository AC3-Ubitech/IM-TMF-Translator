apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer
  namespace: ${ApplicationName}
  labels:
    app: transfer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer
  template:
    metadata:
      labels:
        app: transfer
    spec:
      containers:
      - name: transfer
        image: ${Image}